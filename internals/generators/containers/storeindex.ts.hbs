/**
 * 注入到全局store容器中
 */
import {{ pascalCase newStore }} from './{{ pascalCase newStore }}'

let instance = new {{pascalCase newStore}}()
instance.injectStore()

export default instance

/**
 * 热更新示例
 */
if (module.hot) {
  module.hot.accept('./{{pascalCase newStore}}', () => {
    const state = instance.serialize()
    instance.destory()
    const newInstance = new {{pascalCase newStore}}()
    newInstance.deserialize(state)
    newInstance.injectStore()
    instance = newInstance
  })
}
